{
  "name": "solana-marketplace",
  "version": "1.0.0",
  "description": "Decentralized marketplace on Solana with crypto payments and token gating",
  "private": true,
  "workspaces": [
    "app",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd app && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd app && npm run build",
    "install:all": "npm install && cd app && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
