{
  "name": "solana-marketplace",
  "version": "1.0.0",
  "description": "For Sale By Degen ($FSBD) — Anonymous decentralized marketplace on Solana with crypto payments and tiered access",
  "private": true,
  "workspaces": [
    "app",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd app && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd app && npm run build",
    "install:all": "npm install && cd app && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
