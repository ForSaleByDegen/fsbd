'use client'

import React from 'react'

const MatrixRain = () => {
  return (
    <div className="matrix-rain fixed inset-0 pointer-events-none z-[-1]">
      {/* CSS handles the ::before and ::after pseudo-elements */}
    </div>
  )
}

export default MatrixRain
