'use client'

import React from 'react'

const MatrixRain = () => {
  return (
    <div className="matrix-rain fixed inset-0 pointer-events-none z-[-10] opacity-20">
      {/* CSS handles the ::before and ::after pseudo-elements - subtle green code rain */}
    </div>
  )
}

export default MatrixRain
